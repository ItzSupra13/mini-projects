<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body class="bg-zinc-900">
    <div class="w-full h-full bg-zinc-900 pb-5 pl-4 text-white">
        <div class="nav text-xl tracking-tighter pt-5">
            <a href="/" class="text-blue-300 ml-2">Home</a>
        </div>
        <h1 class="text-3xl tracking-tighter">All Users</h1>
        <div class="users flex gap-3 flex-wrap">
        <% users.forEach((user) => { %>
            <div class="user w-72 p-5 bg-zinc-800 rounded-lg mt-5">
                <div class="w-full h-52 rounded-lg bg-zinc-700"><img src="<%= user.image%>"></div>
                <h3 class="text-2xl tracking-tighter mt-2"><%=user.name%></h3>
                <h5 class="text-zinc-400"><%=user.email%></h5>
                <div class="flex gap-4 mt-3">
                    <a class="text-zinc-600 hover:text-zinc-500" href="">Edit user</a>
                    <a class="text-red-900 hover:text-red-700" href="/delete/<%=user._id%>">Delete user</a>
                </div>
            </div>
            <% });%>
        </div>
    </div>
</body>
</html>