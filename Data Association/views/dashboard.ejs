<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body class="w-full h-full bg-zinc-900 p-5 text-white">
    <div class="w-full p-5 flex justify-end items-center">
        <a class ="bg-red-500 rounded-md px-3 py-2" href="/logout">Logout</a>
    </div>
    <h3 class="text-3xl text-white">Hi <%= user.username %> 😊</h3>
    <p class="text-zinc-500">Create a new post</p>
    <form action="/post" method="post">
        <input class="p-3 outline-none w-1/3 rounded-md bg-transparent border-2 resize-none border-zinc-800 my-3" type="text" name="post" id="post" placeholder="What's on your mind?">
        <input class="px-3 py-2 outline-none rounded-lg m-3 text-white bg-blue-500" type="submit" value="Post">
    </form>
    <h3 class="text-zinc-400">Your Posts:</h3>
    <div class="posts mt-5">
        <div class="postcontainer flex flex-wrap gap-8">
            <% user.posts.reverse().forEach((post) => { %>
            <div class="post p-4 rounded-md w-1/4 border-2 border-blue-600 border-opacity-10 bg-zinc-800">
                <h4 class="text-blue-400">@<%= user.username%> </h4>
                <p class="text-sm tracking-tight"><%= post.content %></p>
                <div class="btns flex gap-5">
                    <a href="/like/<%=post._id%>" class="text-zinc-400">
                        <span class="text-blue-400" ><%= post.likes.length %></span> <%= post.likes.indexOf(user._id)==-1 ? "Like":"Unlike";%>
                    </a>
                    <a href="" class="text-zinc-500">Edit</a>
                </div>
            </div>
            <% }) %>
        </div>
    </div>
</body>
</html>